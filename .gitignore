# Generated by XcodeGen — recreate with `xcodegen generate`
OpenWebUI.xcodeproj/

# Build output
build/
build-device/
DerivedData/
*.xcuserstate
xcuserdata/

# Swift Package Manager
.swiftpm/
.build/
SourcePackages/

# Downloaded on-device models (Whisper GGUF / CoreML / PocketTTS) — never commit
*.bin
*.mlmodelc/
Models/

# Scratch
.shots/
.tmp_shots/
# Release screenshot captures (per version) — the App Store copies live in the
# deliver pack, not here.
prints*/

# macOS
.DS_Store
